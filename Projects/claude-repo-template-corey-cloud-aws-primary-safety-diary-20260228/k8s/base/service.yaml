apiVersion: v1
kind: Service
metadata:
  name: claude-template
  namespace: claude-template
  labels:
    app.kubernetes.io/name: claude-template
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: claude-template
  ports:
    - name: http
      port: 80
      targetPort: 3000
