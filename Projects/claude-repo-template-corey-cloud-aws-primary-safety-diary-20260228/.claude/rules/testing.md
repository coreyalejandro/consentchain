# Testing Rules

- New behavior requires a test.
- Bug fix requires a regression test.
- Keep tests deterministic (no clock/time/randomness without control).
- Prefer unit tests for logic; add integration tests for workflows.
