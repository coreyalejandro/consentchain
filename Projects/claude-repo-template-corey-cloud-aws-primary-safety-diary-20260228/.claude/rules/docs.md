# Documentation Rules

- Every tutorial step includes:
  1. exact file path
  2. exact command
  3. expected output
  4. common mistake
  5. recovery step
- Any diagram must have a text equivalent.
- Keep `CLAUDE.md` under ~200 lines; move detail into `docs/`.
