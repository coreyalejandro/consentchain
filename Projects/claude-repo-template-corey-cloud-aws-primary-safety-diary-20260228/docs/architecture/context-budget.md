# Context Budget Strategy

- Keep `CLAUDE.md` short and stable.
- Put deep reference content in `docs/`.
- Put action rules in `.claude/rules/`.
- Put repeatable workflows in `.claude/skills/`.
- Put guarantees in `.claude/hooks/` + `scripts/`.

**Goal:** performance stays high even as repo grows.
